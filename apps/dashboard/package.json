{
  "name": "dashboard",
  "version": "0.1.0",
  "private": true,
  "description": "Admin dashboard skeleton. No logic, no mock, no random.",
  "scripts": {
    "dev": "echo 'Dashboard skeleton. Implement your logic.' && exit 0"
  }
}
