[project]
name = "concept-gap"
version = "0.1.0"
dependencies = [
  "fastapi",
  "uvicorn[standard]",
  "weaviate-client",
]

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"
